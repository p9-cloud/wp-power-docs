# 規格重新生成紀錄

- **日期**: 2026-03-12
- **模式**: 完全重新生成（非增量更新）
- **來源**: 從現有程式碼推導，非從用戶 idea 開始

## 工作摘要

### Strategic 模式

1. 使用 Serena MCP 讀取所有 PHP 後端和 TypeScript 前端程式碼
2. 識別出 5 個 Actors: Admin, Subscriber, Guest, WooCommerce, WordPress
3. 建立 2 個 Activity: 知識庫管理（7 步）、知識庫授權與存取（9 步 + 決策）
4. 生成 18 個 Feature Files（3 個領域）:
   - doc/ (8): 建立、更新、刪除、複製、排序、查詢列表、查詢詳情、前台檢視
   - product/ (5): 查詢商品、綁定、解除綁定、更新綁定設定、訂單自動授權
   - user/ (4): 查詢用戶、開通權限、撤銷權限、更新期限
   - compatibility/ (1): 相容性遷移
5. F1-F6 面向覆蓋率掃描全部 Clear

### Tactical 模式

6. 從 .feature 推導 api.yml（15 個 REST endpoint + 3 個非 API 功能）
7. 從 .feature 推導 erm.dbml（8 張資料表 + 6 個 Enum）
8. 更新 es.md（14 個 Commands + 5 個 Read Models）

## 從程式碼發現的新功能

以下 3 個 Command 在原有 spec/es.md 中不存在，從前端元件 GrantUsers、RevokeUsers、UpdateGrantedUsers 推導發現：

- GrantUsers（開通知識庫權限）- `POST /limit/grant-users`
- RevokeUsers（撤銷知識庫權限）- `POST /limit/revoke-users`
- UpdateGrantedUsers（更新用戶知識庫期限）- `POST /limit/update-users`

## 所有便條紙

本次無殘留便條紙。所有推斷均基於程式碼實作，標記為 [假設] 的項目已在 F1-F6 掃描中清零。
